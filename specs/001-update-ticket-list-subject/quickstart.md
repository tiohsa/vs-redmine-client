# Quickstart: チケット一覧件名更新

## Prerequisites

- Node.js (project standard)
- npm

## Install

```bash
npm install
```

## Test

```bash
npm test
npm run lint
```

## Validation

- チケット一覧で対象チケットを選択したまま件名を変更して保存する
- 一覧の該当行だけ件名が更新され、選択状態と並びが変わらない
- 保存失敗時は一覧の件名が変わらない
